<%= form_with(model: student) do |form| %>
  <% if student.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
        <% student.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
<div class="forms forms-student">
  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div class="grid_column_2">
    <%= form.label :cpf, style: "display: block" %>
    <%= form.text_field :cpf %>
  </div>

  <div>
    <%= form.label :birth_date, style: "display: block" %>
    <%= form.date_field :birth_date %>
  </div>

  <div>
    <%= form.label :phone, style: "display: block" %>
    <%= form.number_field :phone %>
  </div>

  <div>
    <%= form.label :gender, style: "display: block" %>
    <%= form.select :gender, ["M", "F"], { prompt: "Select a Gender" }, { class: "select_institution" } %>
  </div>

  <div>
    <%= form.label :bill_payment_type, style: "display: block" %>
    <%= form.select :bill_payment_type, ["Cartão", "Boleto"], { prompt: "Select a Student" }, { class: "select_institution" } %>
  </div>

  <div class="forms-button">
    <%= link_to "Cancel", :back, class:"button button-cancel" %>
  </div>

  <div class="forms-button">
    <%= form.submit "Confirm", class:"button button-confirm" %>
  </div>
</div>
<% end %>
